# Phase 2 完成记录 (T30–T45)

> **读取说明**：此文件仅在需要了解 Phase 2 具体实现细节时按需读取。
> 接口详情请直接读取对应的 `.h` 头文件，本文件聚焦设计决策与已知限制。

---

### T30 — ViewManager 视图管理器 (2026-03-29)

**产出文件**: `src/visualization/ViewManager.h/.cpp` · `tests/test_view_manager.cpp`

**接口**: → `src/visualization/ViewManager.h`

**设计决策**:
- ViewManager 使用非拥有指针注入（`setVsgComponents`），不拥有 VSG 组件生命周期
- 相机状态缓存 `viewStateCache_` 以 workbenchId 为 key，工作台切换时自动 save/restore
- 所有 Category（Pipe/Support/Beam 等）默认可见
- `ViewportType` 枚举: `Vsg`（Phase 1–2）和 `Vtk`（T42 后启用）

**已知限制**:
- captureImage 为空实现（预留接口）
- Triad 显隐仅记录状态，与 VSG Switch 的同步依赖应用层调用

---

### T31 — ComponentCatalog 管件目录 (2026-03-29)

**产出文件**: `src/engine/ComponentTemplate.h` · `src/engine/ComponentCatalog.h/.cpp` · `src/engine/templates/*.h`（8 个模板头文件）· `tests/test_component_catalog.cpp`

**接口**: → `src/engine/ComponentCatalog.h`, `src/engine/ComponentTemplate.h`

**设计决策**:
- ComponentCatalog 为单例，8 种管件模板在构造函数中自动注册
- `ComponentParams::extra = unordered_map<string,double>`，扩展参数无需修改基类
- `buildShape()` 统一在原点生成，轴线沿 Z 轴
- 8 种模板：Pipe / Elbow / Tee / Reducer / GateValve / WeldNeckFlange / RigidSupport / SpringHanger
- GeometryDeriver 对 GateValve 改为查 Catalog 而非直接调用 ValveBuilder

**已知限制**:
- 仅 GateValve 接入 Catalog；其余管件仍直接调用 Builder
- 参数规则均为硬编码比例（符合当前规格驱动早期阶段）

---

### T32 — Load 载荷数据模型 (2026-03-29)

**产出文件**: `src/model/Load.h` · `src/model/DeadWeightLoad.h` · `ThermalLoad.h` · `PressureLoad.h` · `WindLoad.h` · `SeismicLoad.h` · `DisplacementLoad.h` · `ForceLoad.h` · `tests/test_load_model.cpp`

**接口**: → `src/model/Load.h` 及各子类头文件

**设计决策**:
- Load 为抽象基类（继承 DocumentObject），纯虚 `loadType()` 区分子类
- 7 种子类：DeadWeight / Thermal / Pressure / Wind / Seismic / Displacement / Force
- 所有 setter 在值未变化时不触发 changed 信号（Signal 语义一致性）
- `affectedObjectIds: vector<UUID>`，一个 Load 可关联多个管点

**已知限制**:
- 无序列化（T43 负责）
- 无组合运算执行逻辑（该逻辑属于求解器模块）

---

### T33 — LoadCase/LoadCombination (2026-03-29)

**产出文件**: `src/model/LoadCase.h` · `src/model/LoadCombination.h` · `tests/test_loadcase.cpp`

**接口**: → `src/model/LoadCase.h`, `src/model/LoadCombination.h`

**设计决策**:
- 全部 header-only（无 .cpp 文件）
- `addEntry`/`addCaseEntry` 通过 `std::find_if` 防重复插入
- DAG 层次：LoadCombination → LoadCase → Load，全部通过 UUID 引用（无裸指针循环）
- `CombineMethod` 枚举：Algebraic / Absolute / SRSS / Envelope
- `StressCategory` 枚举：Sustained / Expansion / Occasional / Operating / Hydrotest

**已知限制**:
- 无序列化（T43 负责）
- 无组合运算执行逻辑

---

### T34 — DesignWorkbench (2026-03-29)

**产出文件**: `src/app/DesignWorkbench.h/.cpp` · `tests/test_design_workbench.cpp`

**接口**: → `src/app/DesignWorkbench.h`

**设计决策**:
- `name() = "Design"`；`viewportType() = ViewportType::Vsg`
- 4 个 toolbar 操作：add-pipe/add-fitting/add-support/add-accessory
- 4 个 panel IDs：DesignTree / Viewport3D / ComponentToolStrip / ParameterPanel
- DesignWorkbench 与 CadWorkbench 并存（CadWorkbench 保留，已有测试依赖其接口）
- activate/deactivate 目前空实现

**已知限制**:
- toolbar 操作均为占位（回调留给 T41 ComponentToolStrip 接入）

---

### T35 — SpecWorkbench (2026-03-29)

**产出文件**: `src/app/SpecWorkbench.h/.cpp` · `tests/test_spec_workbench.cpp`

**接口**: → `src/app/SpecWorkbench.h`

**设计决策**:
- `name() = "Specification"`；`viewportType() = ViewportType::Vsg`
- 5 个 toolbar 操作：new-spec/edit-spec/duplicate-spec/import/export
- 4 个 panel IDs：SpecTree / MaterialTable / ComponentTable / PropertyPanel

**已知限制**:
- toolbar 操作均为占位（规格管理业务逻辑待后续开发）

---

### T36 — DesignTree + ParameterPanel QML 重构 (2026-03-29)

**产出文件**: `ui/panels/DesignTree.qml` · `ui/panels/ParameterPanel.qml` · 更新 `PropertyPanel.qml` · 更新 `PipePointTable.qml` · 更新 `main.qml`

**接口**: → `ui/panels/DesignTree.qml`, `ui/panels/ParameterPanel.qml`

**设计决策**:
- ParameterPanel 作为右侧统一容器，垂直组合 PipePointTable（上）+ PropertyPanel（下）
- PropertyPanel 新增 `editMode: bool` 属性：false=只读文本，true=输入框
- 保留 `ensureExpandedAndFlash()` 链路（T19 设计决策）
- DesignTree 在左侧，内部用 TreeView 渲染 SegmentTreeModel

**已知限制**:
- DesignTree 拖拽排序为占位

---

### T37 — OCCT→VTK 网格转换 (2026-03-29)

**产出文件**: `src/vtk-visualization/OcctToVtk.h/.cpp` · `src/vtk-visualization/BeamMeshBuilder.h/.cpp` · `tests/test_occt_to_vtk.cpp`

**接口**: → `src/vtk-visualization/OcctToVtk.h`, `src/vtk-visualization/BeamMeshBuilder.h`

**设计决策**:
- `toVtkPolyData()` 复用 `geometry::ShapeMesher::mesh()`，顶点法线写入 `vtkPointData::SetNormals()`
- `buildBeamMesh()` 生成梁的线框多边形数据到 `vtkPolyData`
- VTK 依赖仅限 `VTK::CommonCore + VTK::CommonDataModel`（不引入 GPU/绘制组件）

**已知限制**:
- 只输出 vtkPolyData，vtkMapper/vtkActor 绑定在 T38 完成

---

### T38 — VTK 场景管理 (2026-03-29)

**产出文件**: `src/vtk-visualization/VtkSceneManager.h/.cpp` · `tests/test_vtk_scene.cpp`

**接口**: → `src/vtk-visualization/VtkSceneManager.h`

**设计决策**:
- 使用 `uuid → ActorEntry (vtkSmartPointer<vtkActor>)` 映射管理：addActor/updateActor/removeActor
- `updateActor` 先 RemoveActor 旧对象再 AddActor 新对象（避免残留旧 mapper）
- `RenderMode` 切换通过 `vtkActor::SetVisibility()` 完成，不重建场景
- Actor 分类通过 vtkPolyData 拓扑推断：含 Polys → Solid；仅含 Lines → Beam

**已知限制**:
- 载荷箭头/vtkVolume 等特殊 prop 类型未纳入当前管理

---

### T39 — 工作台切换 + QML 动态加载 (2026-03-29)

**产出文件**: `src/app/AnalysisWorkbench.h/.cpp`（骨架）· 更新 `src/ui/WorkbenchController.h/.cpp` · 更新 `ui/main.qml` · 更新 `ui/panels/TopBar.qml` · 更新 `src/main.cpp` · `tests/test_workbench_switch.cpp`

**接口**: → `src/ui/WorkbenchController.h`（`activePanelIds`、`switchWorkbench(name)` Q_INVOKABLE）

**设计决策**:
- QML 中用 `Repeater` 根据 `WorkbenchController.activePanels` 动态生成 `Loader`
- `viewportLoaded` 信号配合 `Component.onCompleted`，避免 VsgQuickItem 未初始化时注入空指针
- test_workbench_switch.cpp 使用 Dummy Test（编译 OOM 限制，功能验收通过 T44/T45 覆盖）

**已知限制**:
- Dummy Test（资源限制，完整单元测试留后续）

---

### T40 — StatusBar + 右键菜单 + 框选 (2026-03-29)

**产出文件**: 更新 `src/ui/AppController.h/.cpp` · 更新 `src/visualization/PickHandler.h/.cpp` · 更新 `src/visualization/SceneManager.h/.cpp` · 更新 `ui/panels/StatusBar.qml` · `ui/components/ContextMenu.qml` · 更新 `ui/panels/Viewport3D.qml` · 更新 `ui/main.qml` · `tests/test_statusbar_contextmenu.cpp`

**接口**: → `src/ui/AppController.h`（`statusText`、`mouseWorldPos` Q_PROPERTY）, `src/visualization/PickHandler.h`（`boxSelect()`）

**设计决策**:
- StatusBar 三区：左=选中对象名，中=鼠标 3D 坐标，右=摄像机缩放级别
- 框选：左→右 = Window 选（对象完全在框内），右→左 = Crossing 选（对象部分在框内）
- `boxSelect` 使用 `vsg::ComputeBounds` + AABB 8角点投影到屏幕坐标进行 2D 空间过滤
- `multiSelect` 支持 Shift 追加模式

**已知限制**:
- 框选 AABB 8角点投影方法在极端透视角度下可能有误判

---

### T41 — ComponentToolStrip (2026-03-29)

**产出文件**: `ui/panels/ComponentToolStrip.qml` · 更新 `src/ui/AppController.h/.cpp` · 更新 `tests/test_qml_ui_panels.cpp`

**接口**: → `ui/panels/ComponentToolStrip.qml`, `AppController::insertComponentRequested` 信号

**设计决策**:
- 固定宽度 68px 双列图标条：左列 Fittings (6 种)，右列 Accessories (5 种)
- Unicode 字符作图标，tooltip 提供中文说明
- 实际插入逻辑通过 `AppController::insertComponentRequested(typeName)` 信号传到上层

**已知限制**:
- 图标为 Unicode 字符，非矢量图，在高 DPI 屏幕可能模糊

---

### T42 — VTK-QML 桥接 (2026-03-29)

**产出文件**: `src/vtk-visualization/VtkViewport.h/.cpp` · `ui/panels/VtkViewport.qml` · `tests/test_vtk_qml.cpp`

**接口**: → `src/vtk-visualization/VtkViewport.h`

**设计决策**:
- 使用 `QQuickFramebufferObject + VtkFboRenderer`，`vtkGenericOpenGLRenderWindow` 挂载到 Qt Quick
- 在 `initializeGL()` 阶段（非构造函数）调用 `interactor_->Initialize()`，避免 OpenGL Context 未就绪导致 SIGSEGV
- `ViewManager::setVtkComponents(VtkSceneManager*, VtkViewport*)` 接口由 T42 添加

**已知限制**:
- VtkFboRenderer 在多次快速调整窗口大小时偶发重建延迟（VTK 通用问题）

---

### T43 — 序列化扩展（Load/LoadCase/LoadCombination）(2026-03-29)

**产出文件**: 更新 `src/app/ProjectSerializer.h/.cpp` · 更新 `src/app/DependencyGraph.h/.cpp` · 更新 `src/app/TransactionManager.cpp` · `tests/test_load_serialization.cpp`

**接口**: → `src/app/ProjectSerializer.h`（新增 `loads`/`loadCases`/`loadCombinations` JSON 节点）, `src/app/DependencyGraph.h`（`rebuildLoadDependencyChain()`）

**设计决策**:
- JSON 结构追加 `loads / loadCases / loadCombinations` 三个顶层节点
- 按 Load → LoadCase → LoadCombination 顺序恢复；UUID 引用无效时跳过该条目（容错设计）
- `DependencyGraph::rebuildLoadDependencyChain()`：load/loadCase/combo 全 dirty，统一排序后 recompute
- TransactionManager 扩展载荷字段回放：operatingTemp / pressure / 方向分量 / 位移 / 力矩分量

**已知限制**:
- 不包含 LoadCase::entries 与 LoadCombination::caseEntries 增删操作的 undo/redo 回放

---

### T44 — AnalysisWorkbench (2026-03-29)

**产出文件**: `src/app/AnalysisWorkbench.h/.cpp` · `ui/panels/AnalysisTree.qml` · `ui/panels/LoadTable.qml` · `ui/panels/LoadCaseTable.qml` · `tests/test_analysis_workbench.cpp`

**接口**: → `src/app/AnalysisWorkbench.h`

**设计决策**:
- `name() = "Analysis"`；`viewportType() = ViewportType::Vtk`
- 5 个 toolbar 操作：toggle-render-mode / add-load / manage-loadcase / run-analysis / show-results
- 5 个 panel IDs：AnalysisTree / VtkViewport / LoadTable / LoadCaseTable / PropertyPanel
- `renderMode_` 在 deactivate 时保留状态，下次 activate 自动恢复

**已知限制**:
- run-analysis / show-results 为占位实现（求解器集成待后续 Phase 3）
- Renders：VTK actor 可见性切换依赖 VtkSceneManager 的 setRenderMode

---

### T45 — Phase 2 端到端集成测试 (2026-03-29)

**产出文件**: `tests/test_phase2_integration.cpp`（15 个测试覆盖全部 Phase 2 验收场景）

**接口**: → `tests/test_phase2_integration.cpp`（Phase2IntegrationTest 夹具）

**设计决策**:
- GTest fixture 搭建完整 Phase 2 栈：Document + DependencyGraph + TransactionManager + RecomputeEngine + WorkbenchManager
- 15 个测试覆盖：Load CRUD / LoadCase组合 / 序列化 round-trip / WorkbenchManager 切换 / ViewManager 状态 / ComponentCatalog 查表 / 框选 API / STEP 反读验证
- 最终构建结果：35/35 tests passed（含 Phase 1 集成测试 12 个）

**已知限制**:
- QML 交互层无法在纯 C++ 测试中验证（T36/T40/T41/T44 QML 部分通过代码审查确认）
- run-analysis/show-results 测试为占位（对应 AnalysisWorkbench 业务逻辑待 Phase 3）
