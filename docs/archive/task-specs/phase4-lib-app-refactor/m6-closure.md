# M6 收口与模板化

## T75 清理旧目录兼容层

**目标**
逐步移除旧路径与临时兼容入口。

**依赖**
- T62
- T64
- T65
- T66
- T74

**交付物**
- 兼容层清理结果

**验收标准**
- `src/lib` 与 `src/apps` 成为唯一真实结构

## T76 固化 pipecad app 模板

**目标**
将 `pipecad` app 提炼为未来多个 app 的参考模板。

**依赖**
- T62
- T75

**交付物**
- app 模板化结构说明
- app 目录模板

**验收标准**
- 新增 app 时无需再重新设计 lib 边界

## T77 更新文档与开发规范

**目标**
使文档、构建说明、测试入口与新结构一致。

**依赖**
- T75

**交付物**
- 更新后的架构文档
- 更新后的开发流程说明
- 更新后的任务接力说明

**验收标准**
- 开发者仅依赖新结构即可继续开发