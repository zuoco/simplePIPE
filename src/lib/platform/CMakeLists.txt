# Phase 4 / lib::platform — 平台封装层
# 子域：occt（几何内核）/ vsg（渲染引擎）/ vtk（分析可视化）
# geometry、visualization、vtk-visualization 迁入时间线：T59、T60
# 当前无源文件，CMakeLists.txt 作占位用。

add_subdirectory(occt)
add_subdirectory(vsg)
add_subdirectory(vtk)
